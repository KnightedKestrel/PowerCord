General:

Added tsconfig.json and adjusted imports in csvProcessor.ts, database.ts and run-script.ts 

run-script.ts:

Added lines to create Database.
Other files were trying to create columns and check if they existed without making the DB first. 

csvProcessor.ts:

Changed batch processing logic to one row at time.